$kind: http-request
url: "{{baseUrl}}/api/v1/courses"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "title": "Incremental Draft Course",
      "categoryId": "{{categoryId}}"
    }
order: 14000
