$kind: http-request
url: "{{baseUrl}}/api/v1/admin/instructors/reject"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "reason": "Insufficient credentials verification."
    }
order: 2000
